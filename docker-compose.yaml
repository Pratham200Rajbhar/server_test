services:
  app:
    build:
      context: .
      target: runner # <-- Fixed: changed from 'docker_target' to 'target'
    ports:
      - "3004:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped